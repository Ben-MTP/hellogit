git config --list

set: username, email, pass:
git config --global user.username ManhKM
git config --global user.email minhmanh.office.uet@gmail.com
 
remove user,pass,emai:
git config --list
remove pass: git config --global --unset user.password

clone a reposetory:
create folder --> save reposetory
git clone link_reponsetory

clone and rename:
git clone link_reponsetory other_name

checking status file:
git status




--------
create a new repository on the command line
git init
git add README.md
git commit -m "first commit"
git remote add origin link_reponsitory
git push -u origin master

or push an existing repository from the command line
git remote add origin link_reponsitory
git push -u origin master


git log
git log --oneline --> commit in oneline

---------: Tìm commit bị lỗi
git reset --> remote con tro ve mot vi tri nao do
git reset ma_log_commit. quay trở lại commit theo con tro HEAD chỉ định.
Note*: git reset --hard ma_log_commit


---------: Lệnh git checkout[mã commit]
liệt kê commit trươc đó

---------: Lệnh xem N lệnh commit sau cùng
git log -n
git checkout master


---------: Git commit --amend
Thêm những file mới vào những commit tồn tại trước đó
git commit --amend

---------: Xem nội dung thay đổi của từng commit
git log -p
Enter Q --> to exit terminal

---------: Khi add những file chuẩn bị cho commit sắp tới
nhưng có cách nào có thể loại bỏ được một số file không mong muốn không?
-> add file
git reset HEAD -- name_file
git status
remove từng file một, không list một phát hết các file

---------: Tạo ra một nhánh mới trong local- create git branch
local
remove --> trên server
kiểm trên trên máy local có bao nhiêu nhánh đang được tiến hành: git branch
dùng dấu gạch dưới
Note*: những gì đang trình bày được thao tác trên git local - tức là trên máy tính cá nhân
khi sử dụng trong remove thì sẽ được sử dụng sau

---------: Đổi tên một nhánh trong git:
+ nếu đứng tại nhánh đó đổi tên chính nhánh đó
git branch -m new_name_branch

+ nếu đứng tại nhánh khác và đổi tên của một nhánh khác
git branch -m old_name_branch new_name_branch





--------
git status
git add .  --> change status file in reponsitory

git add . --> all file
git add -u --> file truoc do
git add -A --> all file

chuan bi dua file vao lich su
chup man hinh chuan bi luu
xanh --> san sang cho viec commit

git commit --> xac nhan tin tuc

git commit -a -m "mess fix something" (add + commit)




